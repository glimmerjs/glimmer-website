'use strict';

module.exports = function(environment) {
  let ENV = {
    modulePrefix: 'glimmer-website',
    environment: environment,
    rootURL: '/',
    locationType: 'auto'
  };

  return ENV;
};
